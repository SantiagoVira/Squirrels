{
    "name": "Squirrels",
    "version": "1.0.0",
    "scripts": {
        "build": "cd client && npm install && npm run build"
    },
    "cacheDirectories": [
        "client/node_modules"
    ]
}
